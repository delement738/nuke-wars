# Nuke Wars — Project Instructions

## What this project is
A 1v1 web-based strategy game: simultaneous hidden orders, hex-grid maneuver, drone reconnaissance, fully deterministic combat (no dice — dread comes from hidden information), and a hidden-bunker decapitation endgame ("commit → dread → reveal" is the core loop).

**The full game design is in `docs/nuke-wars-v1-spec.md`. Read it before implementing any game rule, and treat it as the source of truth for all mechanics, numbers, and V1 scope.** If an implementation decision would change a rule or number in the spec, say so explicitly and update the spec file as part of the change.

**The design was pivoted on 2026-08-11** (single missile type, unlimited munitions, recon drone, decoy bunker, per-round intercept cap, hits-based damage, deterministic combat). The spec is fully post-pivot; spec section *numbers* §1–§9 kept their old meanings so code citations stay valid, and §10–§12 are new. **But §8's build-order step numbers changed** — old step 2 was "simulation engine core"; new step 2 is the pivot migration. Existing code comments citing "build-order step 2" mean the old numbering; fix them as you touch each file. Systems cut in the pivot (and the decoy, promoted back in) are listed at the top of `docs/v2-backlog.md`.

`docs/v2-backlog.md` is deferred-feature reference only — never implement from it.

## Stack
- TypeScript + Vite + React 19
- PixiJS v8 for the game map/rendering
- Zustand for client state
- Hex math is hand-rolled in `src/sim/hex.ts` (axial coords) — deliberately dependency-free so the sim engine can move server-side unchanged in V1.5
- Later (V1.5, build-order step 11+): Node.js WebSocket server (not yet in the project)

## Architecture rules (non-negotiable)
- `src/sim/` — pure simulation engine. NEVER import React, Pixi, DOM APIs, or anything network-related here. All game rules, state, and resolution live here as pure functions.
- `src/state/` — client state (Zustand). **The only module that ever holds an unfiltered `GameState`**, in a module-private variable with no accessor, so `src/render/` and `src/ui/` are structurally incapable of seeing the truth (spec §6 layer 2). May import `src/sim/`; `src/sim/` may never import it. React lives only in `state/useMatch.ts` — the store itself is a plain testable object.
- `src/render/` — PixiJS drawing only. Reads state, draws it. Never mutates game state. The type flowing in is `VisibleGameState` and nothing else.
- `src/ui/` — React HUD/menus. Same rule: reads state, sends player intents.
- **V1 combat is deterministic BY DESIGN — `resolve()` uses no randomness at all.** The seeded RNG (`makeRng` in `src/sim/map.ts`) is used only for map generation. `resolve()` keeps a `seed` parameter for forward compatibility but nothing in V1 resolution may read it (documented in spec §6 — don't "fix" this by adding RNG). No `Math.random()` anywhere in sim code, ever. Every simultaneous tie has a written tiebreak in the spec (§10); determinism bugs are iteration-order bugs.
- Game stats/definitions (units, terrain, rule numbers) go in plain data tables keyed by string IDs in `src/sim/defs.ts`, not hardcoded logic branches.
- Combat/resolution animates from the emitted event log, not by diffing states. Event visibility (which player may see which event) is specified in spec §6 — respect it in the visibility filter, never in resolve().
- **`resolve()` never lies; the visibility filter does.** The decoy bunker must be indistinguishable from the real one to the enemy, but the sim always stores and emits the truth (`kind: 'decoy'`). The decoy→bunker mask is applied *only* in `filterForPlayer`/`filterEventsForPlayer` (spec §6, §12). The visibility filter is the single place in the codebase permitted to know a decoy is a decoy.
- **Detection rules (spec §11) — four rules, no exceptions.** (1) The map is public and your own assets are always visible. (2) An enemy asset appears on your map only via one of exactly two detectors: the recon drone's swath, or automatic launch detection. (3) A launcher sighting lasts **one round** (the next order phase, then it's cleared — it can relocate); a static sighting (bunker, decoy, interceptor base) is **permanent** until public destruction. (4) No detection distinguishes the real bunker from the decoy. There is no vision radius around units, no line of sight, and no persistent "last seen" ghost markers — that history lives in the event log, which is permanent.
- **The indistinguishability principle (spec §12):** every rule that mentions the bunker must apply identically to the decoy, with hit points the only exception. Any asymmetry becomes a rules-derived tell that identifies the real bunker for free. When writing a new rule that touches the bunker, ask whether it must also name the decoy — the default answer is yes.

## Workflow rules
- One system per session. Confirm the session goal before writing code, and don't touch systems outside the stated boundary.
- For sim-engine work: define/adjust TypeScript types first and show them for approval before implementing logic.
- Every sim-engine feature ships with Vitest unit tests: normal case, edge cases, and at least one illegal-order case.
- Prefer small, verifiable steps. After changes, tell me exactly how to verify (what command to run, what I should see in the browser).
- Don't add new dependencies without asking first.
- At the end of each session, update the "Current status" section below.

## Working with me
- I'm a coding beginner. Explain what you're doing and why in plain terms as you go — teach, don't just produce.
- Primary machine is Windows (project at `C:\Users\stirl\nuke-wars`), but I also work remotely from a MacBook. Check which OS the current session is actually running on before giving shell commands — don't assume Windows.
- Walk me through any manual steps click-by-click.
- If something I ask for conflicts with the spec or architecture rules, push back and explain the trade-off instead of silently complying.

## Commands
- `npm run dev` — start dev server (localhost:5173)
- `npm run build` — production build / full type-check
- `npm run lint` — ESLint
- `npm test` — Vitest, single run (`npm run test:watch` for watch mode)
- `npm run soak` — the balance harness: plays whole CPU-vs-CPU matches and prints outcome distribution, recon coverage and offensive stats. `SOAK_MATCHES=100 npm run soak` for tighter numbers, `SOAK_SEED=7` for a different family of boards. **Run this before and after any change to `defs.ts` or `cpu.ts`** — it is the only thing that turns spec §7's "untested first drafts" into evidence.

## Current status (update at end of every session)

### ✅ Code and docs are both post-pivot as of 2026-08-11 — no known mismatch

### Completed
- Project scaffold; `src/sim/hex.ts` (axial hex math + odd-q offset↔axial bridge + `hexKey`, unit-tested); `src/sim/map.ts` (symmetric terrain gen with seeded RNG, O(1) `tileAt()` — the mirror became a 180° rotation on 2026-08-11, see below); `src/render/GameCanvas.tsx` (Pixi hex map, flat-top, pan/zoom/hover/select, StrictMode-safe); `src/sim/types.ts` + `src/sim/defs.ts` (pre-pivot versions); `src/sim/movement.ts` (`reachableHexes` cost-aware flood fill + `validateMove`). 47 tests passing. TS `strict` on. Sim layer dependency-free.
- 2026-08-11: **design pivot** — spec rewritten (deterministic combat, drone recon, per-round intercept cap, hits-based damage, secret placement, straight-line flight via `hexLine`); v2-backlog gained the cut-systems ledger; README updated. All seven pre-pivot open design questions resolved (see spec's "Resolved-by-pivot ledger").
- 2026-08-11: **build-order step 2 — pivot migration DONE.** `types.ts` rewritten (5 unit kinds, MOVE/LAUNCH/FLY orders, §6 event shapes with per-round `missileId`, two-part intel, `droneRespawnIn`, `deadHandFor`, `SETUP` phase, `MUTUAL_DISARMAMENT` outcome); `defs.ts` rewritten (`UNIT_DEFS` with hp, `SPAWNS`/`ALL_SPAWN_HEXES`, `RULES` with `ordersPerUnit`/ranges/exclusion/home zones/round cap); `map.ts` forces the 8 spawn hexes to plains (§12); new `map.test.ts`; `movement.test.ts` migrated. Two gaps found and closed: `validateMove` now rejects drone MOVE orders with `AIR_UNIT` (its move 6 is a flight range, not a ground budget), and the movement tests now derive geometry from `UNIT_DEFS.launcher.movement` instead of hardcoding it. Spec amended: `missileId` on the three missile events, `DRONE_RESPAWNED` row, MOVE-is-launcher-only note in §9. 67 tests passing.
- 2026-08-11 (docs only, no logic changed): **detection rules rewritten and simplified.** Spec §11 is now four flat rules (map is public / exactly two detectors / launcher sighting lasts 1 round, static sighting is permanent / nothing tells bunker from decoy). Persistent launcher ghost markers were **cut**; the permanent record is the event log (§6). "Fog of war" retired as a term project-wide — architecture layer 2 is now the **visibility filter**. Touched: spec §2/§6/§7/§9/§11/§12 + ledger, README, v2-backlog, and the comments in `types.ts` and `movement.ts`.
- 2026-08-11 (same session, amendment): **decoy bunker added to V1** — 1 per player, 1 HP, secretly placed, rule-identical to the real bunker except HP (spec §12's indistinguishability principle). Roster is now 5 asset kinds / 8 assets per player. Spec §1/§2/§3/§4/§6/§7/§8/§11/§12 amended; v2-backlog marks the decoy promoted out of deferred (its "false signal emission" behaviour stays V2).

- 2026-08-11: **map axis corrected to north/south.** Territory was split left/right (19w × 15t, mirrored columns); it is now split top/bottom — **P1 holds the south and advances north**, P2 holds the north. Map is **16 wide × 19 tall**. Symmetry changed from a column mirror to a **180° rotation** (`rotate180` in `map.ts`), because a top/bottom mirror is geometrically impossible on flat-top odd-q hexes — verified by brute force over all 92k hex pairs before committing to it. Width must stay even for the half-turn to be an isometry; `generateMap` throws otherwise. `defs.ts`: `SPAWNS` transposed (P1 launchers row 16 / drone row 17; P2 row 2 / row 1 — 14 rows apart, same as the old 14 columns, so §7 tuning is untouched), `homeZoneCols` → `homeZoneRows`. Spec §7 gained a north/south statement and a "why a rotation, not a mirror" note; §8/§11/§12 de-mirrored. `GameCanvas` now fits the (portrait, ~900px) board to the window on first paint. 81 tests passing.

- 2026-08-11: **terrain simplified to two types (`feature/terrain-tiles`).** `urban` is **cut** (flavour-only, no rule; its V2 regime-score role is flagged in `v2-backlog.md`). `Terrain` is now `'plains' | 'mountain'`. Mountains went from ~14% scattered singletons to **15% grown as ranges** — 4 per generated half, walked with a 0.7 straightness bias, then a de-orphaning pass that relocates any lone mountain against an existing range, so "every mountain touches another" is a guaranteed property and not a tendency. Because ridges (unlike noise) can wall a board off, `generateMap` is now **generate → validate → re-roll**: coverage inside 10–20%, all 8 spawns in one connected plains region, and no launcher more than 12 ground-cost from a firing position on the enemy line (8 on open ground — this is the §7 "first blood ~round 3" premise made testable). Re-roll rather than carve, because an asymmetric carve breaks the half-turn. Spawn hexes *and their rings* are now excluded from mountain seeding up front, rather than converted to plains afterwards. **Rules change: bunker, decoy and interceptor bases may be built on mountains**; launchers and drones still spawn and move on plains only. `TerrainDef.passable` → `groundPassable` and new `RULES.placementTerrain` — the rename exists so placement code cannot silently reuse the movement flag. New `defs.test.ts` asserts bunker and decoy allow identical terrain (§12) and that HP is their only difference; `movement.test.ts` cross-checks the new `groundCostsFrom` in `map.ts` against `reachableHexes` so the two flood fills can't drift. Spec §2/§7/§10/§11/§12 + ledger amended. 116 tests passing.

- 2026-08-11: **build-order step 3 — `hexLine()` DONE (`feature/hex-line`).** Cube-lerp line drawing with the pinned epsilon nudge in `hex.ts`, plus private `cubeRound`. Returns both endpoints (`a` first, `b` last, length `distance + 1`) and stays rule-free: no range cap, no terrain, no legality — `hexLine(a, a)` returns `[a]` rather than throwing, because "the drone may not fly to its own hex" is a validator rule. **Spec §10 amended:** the nudge is applied to **both endpoints**, not just `a` — it matches the redblobgames reference the method is cited from (so the V1.5 server can copy standard code and agree), and it makes lines **reversible**, which nudging only `a` does not. §10 also now records the third offset (**-3e-6**, forced by q + r + s = 0), which the two pinned constants imply but never stated. One bug caught by the tests: `Math.round(-0.4)` returns **negative zero**, which is not deep-equal to `0` — left in, it would have broken step 4's determinism test; `cubeRound` now normalises it. 124 tests passing.

- 2026-08-11: **housekeeping, no logic changed (`chore/housekeeping`).** Deleted the seven merged feature/docs branches (local and on GitHub) now that steps 1–3 are all in `main`. Removed four unreferenced Vite-scaffold assets (`src/assets/react.svg`, `vite.svg`, `hero.png`, `public/icons.svg` — the last is the template's social-icon sheet); `src/assets/` is gone entirely and `public/favicon.svg` stays, since `index.html` references it. Cleared the stale "Known issues" entry that still claimed a code/spec mismatch. Verified clean: 124 tests, `eslint`, and `tsc -b` all pass; the `build-order step` citations in `types.ts` were checked against spec §8 and already use post-pivot numbering.

- 2026-08-11: **build-order step 4 — `resolve()` skeleton + ground movement DONE (`feature/resolve-movement`).** New `resolve.ts` exporting the engine's single entry point; new `ResolveResult` in `types.ts`. The five phases of §3 are laid out in order with 1–4 as stubs naming the step that fills each, and each phase rebinding a `working` state so "movement applies to the post-impact board" becomes true by construction when step 6 lands. **The pleasant surprise: three of §9's five rulings needed no code at all.** Because `validateMove` already tests occupancy against the true start-of-phase state, a swap, a follow-into-a-vacating-hex, and an advance into an unseen enemy all fail on their own. Only the standoff (group admitted moves by destination; 2+ claimants cancels all of them) is new logic. Three §9 gaps were found and closed by amendment: over-budget order batches, which failures earn `MOVE_FAILED`, and canonical event ordering (all three in the gotchas below). `_seed` is prefixed and eslint got a one-line `argsIgnorePattern: '^_'` so its being unread is visibly deliberate. 153 tests passing (29 new). Both new invariants were mutation-checked — deliberately breaking the standoff rule and the emission order each fail tests.

- 2026-08-11: **build-order step 5 — recon phase DONE (`feature/recon-drone`).** Two new modules: `coverage.ts` (§10 interceptor geometry — its functions take the **viewer**, never the base owner, so "a base never engages its own side" is enforced by the signature rather than remembered; step 6 extends it with the base-list variant missiles need) and `recon.ts` (`validateFly`/`flyDrone`/`reconSwath`/`droneFor`, with no terrain and no occupancy check anywhere — their absence is the air layer). `resolve.ts` gained phase 1 and the respawn tick, and order budgets are now computed **once** up front and shared by every phase, so a drone handed MOVE+FLY is over budget in phase 1 and phase 5 alike. 205 tests passing (52 new); all seven new invariants mutation-checked. Six spec gaps closed by amendment (hover reveals, silent FLY rejection, `DRONE_MOVED` path semantics, what "reveals nothing from its death hex" actually means, re-sighting keeps the first-seen round, `DRONE_DOWNED` is the drone's only death event). **One code defect found and fixed:** `occupiedHexes` blocked on drones, contradicting §9 — harmless while drones sat on spawn, but from this step on a parked drone would have been a third detector.

- 2026-08-11: **project audit (`chore/project-audit`), no gameplay logic changed.** Full sweep of code, docs, config and repo hygiene. Verified clean: 206 tests, `eslint`, `tsc -b` and `vite build` all pass; `src/sim/` imports nothing but its own modules; no `Math.random()` anywhere; no TODO/FIXME debt; `dist/`, `node_modules/` and the local settings file correctly ignored and nothing stray tracked; 0 npm vulnerabilities; working tree in sync with `origin/main` with no stale branches. Cross-checked every §7 number against `defs.ts` (board 16×19, the four spawn half-turn pairs, 14 rows between launcher lines, 46/304 mountains, home zones, radii, respawn delay, round cap) — all agree. Stress-tested the generator past what the committed tests cover: **400 seeds** produced no throw, no asymmetry, no lone mountain, no blocked spawn, and coverage inside the band every time; `hexLine` stayed reversible, contiguous, correctly-sized and negative-zero-free over 3,600 endpoint pairs. **One real gap found and fixed** (below). Two things deliberately left alone: the README status was stale and is now refreshed, and `GameCanvas` still calls `generateMap()` itself — the render layer owning map creation is an architecture-rule violation that build-order step 9 closes when the Zustand store takes over state.

- 2026-08-11: **owner-only events now name their audience (same audit).** `UNIT_MOVED`, `MOVE_FAILED`, `BUNKER_HIT`, `DRONE_MOVED` and `DRONE_RESPAWNED` are owner-only per §6 but carried nothing except a `unitId`, and `filterEventsForPlayer(events, playerId)` gets no `GameState` to resolve that id against — so **step 8 could not have routed a single one of them.** All five now carry `owner: PlayerId`, matching `DRONE_DOWNED`, which already did. Nothing about audiences, rules or numbers changed; the events merely became self-describing, and the filter stays a pure function of the log (which is what lets a replay be re-filtered without rebuilding each round's state). Spec §6 amended (table rows, a new "every owner-only event names its audience" paragraph, and a ledger entry). New test `names the audience on every owner-only event` exercises both sides and was mutation-checked — hardcoding `owner: 'p1'` in one emission fails it.

- 2026-08-11: **build-order step 6 — launch/intercept/impact phases DONE (`feature/launch-impact`).** New `missiles.ts` (`validateLaunch`, `missileIdFor`, `createMissile`, `canonicalOrder`, `flyMissiles`, `damageByHex`) plus `basesCovering` in `coverage.ts` and `compareHex` in `hex.ts`; `resolve.ts` gained phases 2 and 3, so four of the five §3 phases now exist and "movement is applied against the post-impact board" is true by construction rather than by rule. `RULES.missileDamage` added (the per-missile hit was the last un-tabled rule number). **One spec rule changed:** §10's simultaneous-missile tiebreak is now **ascending origin hex** (`compareHex`), not the firing launcher's unit id — the ordering is publicly observable, so the old rule would have leaked an ordering of enemy launcher ids and with it the cross-round trackable identity §11 keys all intel by hex to prevent (§6 already forbade the same thing in missile ids). Bases keep the id tiebreak: the defender owns them, no event names one, and they cannot move. Six rulings added by amendment (silent rejected LAUNCH; the log's phase grouping and per-phase canonical order; `BUNKER_HIT` written as "bunker *or* decoy"; impact touches ground assets only; `UNIT_DESTROYED` clears that hex from the enemy's map; both detectors filing one hex-keyed contact). 269 tests passing (63 new); seven new invariants mutation-checked. **The §11 open question was settled first (see below).**

- 2026-08-11: **build-order step 7 — dead hand, outcomes and the state machine DONE (`feature/dead-hand-outcomes`).** Two new modules: `outcomes.ts` (§4's table as one pure `adjudicate(state)` returning CONTINUE / DEAD_HAND / OUTCOME — it reads `state.phase`, so the *same* board means "you get a final round" in a normal round and "you lost" in the dead-hand one) and `setup.ts` (§12 placement validation + `startMatch`, the SETUP→ORDER_PHASE edge that builds all 16 units). `resolve.ts` gained phase 4, the dead-hand branch, and the two engine guards; `PLAYERS`/`opponentOf` moved to `types.ts` so resolve, outcomes and step 8's filter share one iteration order. **All five §3 phases now exist.** 335 tests passing (66 new). Both new invariants mutation-checked: letting the decoy count as a bunker fails 3 tests, and running phase 5 after a verdict fails 12. Seven spec gaps closed by amendment (below), one of them a genuine generalisation — **any** phase-4 verdict now stops the round, where §3 had said it only of dead hand.

- 2026-08-11: **build-order step 8 — the visibility filter DONE (`feature/visibility-filter`).** New `visibility.ts` — `filterForPlayer` + `filterEventsForPlayer`, the §6 layer-2 pair — plus five types in `types.ts` (`VisibleGameState`, `VisiblePlayerIntel`, `VisibleStaticReveal`, `SpottedKind`, `VisibleEvent`). **The engine is now feature-complete for V1: sim truth in, per-player redacted views out.** 384 tests passing (49 new); all nine invariants mutation-checked, including the two that matter most — routing `ASSET_SPOTTED` by `owner` instead of `opponentOf(owner)` fails 9 tests, and dropping the decoy mask fails 4. The design principle throughout is **structural redaction**: wherever the output type can make the hidden thing unrepresentable, it does, so a plausible-looking future edit cannot reintroduce a leak. Six spec gaps closed by amendment (§6 never said what the *filtered state* contains, only who may see which event), the load-bearing one being that **enemy units are absent from the filtered state, not masked within it**. One thing deliberately not built: nothing calls the filter yet — step 9's store is its first consumer.

- 2026-08-12: **build-order step 9 — sim wired to the renderer DONE (`feature/store-wiring`).** New `src/state/` layer: `match.ts` (the store), `useMatch.ts` (React bindings), `sandbox.ts` (the dummy opponent's setup); new `src/ui/` layer: `Hud.tsx`, `EventLog.tsx`, `SelectionPanel.tsx`, `eventText.ts`, `hud.css`; `GameCanvas.tsx` rewritten around three effects and split with `render/draw.ts` + `render/geometry.ts`. **The game is now a thing you can look at:** the board draws the viewer's own units, their intel and their bases' coverage, the HUD keeps the running per-player log, and the viewer switch shows the same truth as two different pictures. 414 tests passing (30 new). Verified in a real browser, not only in tests — rounds resolve, the log fills, the viewer flips, a tile click reports its terrain, and resignation ends the match with the buttons disabled.
  - **The standing architecture-rule violation is closed:** `GameCanvas` no longer calls `generateMap()`. The store owns the map and calls `startMatch`.
  - **Gotcha 34 is now structural, not remembered.** `truth` is a module-private `let` in `match.ts` — not in the store, not exported, no accessor — so no hook, component or test *can* obtain a raw `GameState`. Everything that leaves that file has been through `filterForPlayer`/`filterEventsForPlayer`, including the `GAME_OVER` event the store synthesises for a resignation.
  - **`CAPITULATION` now has its setter** (`resign`), the one action that writes `truth` without the engine: a resignation is not a board fact, so `adjudicate` can never return it.
  - **One render bug found by looking at a screenshot** (no test would have caught it): `drawCoverage` painted interceptor bubbles over hexes off the edge of the board, because `hexesInRange` is pure geometry and returns them. `tileAt` is the on-map test — the map is a rectangle in col/row, which is a slanted parallelogram in axial.
  - **Not built, deliberately:** no order builder, no setup UI, no hotseat handoff — all step 10. "Resolve round" submits an empty order list, which is a legal round (launchers hold, drones hover, §3), and `resolveRound(orders)` is the seam step 10 plugs into.

- 2026-08-13: **CPU opponent, three difficulties (`feature/cpu-opponent`).** Not a build-order step — a solo-play/testing feature requested outside the numbered sequence. New `src/state/cpu.ts`: `cpuOrders(view, difficulty, player, rng)` decides one round's `Order[]` from that player's own `VisibleGameState` alone — never `truth` — so the CPU is bound by exactly the fog a human in its seat would be. It turns its redacted view back into a `GameState`-shaped "belief" (own units only, nothing standing in for the enemy) and checks every proposed order against the real `validateMove`/`validateLaunch`/`validateFly` before returning it — one concrete answer to the "UI only has a VisibleGameState" open question step 9 left for step 10, applied to the CPU first since it needed no UI to ship. EASY mostly holds/hovers and ignores intel; MEDIUM advances toward the enemy's home zone and fires at the nearest known target, kind-blind; HARD prefers a launcher contact over a bunker/decoy site when both are in range (a contact is a certain kill this round, §11) and prefers a safe reachable hex over a barely-better exposed one. All three are fully stateless — nothing is remembered between rounds. `resolveRound()` in `match.ts` now calls `cpuOrders` for `SANDBOX_DUMMY` instead of submitting `[]`; a `difficulty` field + `setDifficulty` joined the store, with three buttons in the HUD. 27 new tests (`cpu.test.ts`): legality checked against TRUE state at match start (not just belief), the `ordersPerUnit` invariant, determinism, MEDIUM/HARD target-priority divergence, HARD's movement-safety tolerance, and an illegal-order-avoidance case (degenerate intel that would resolve to a same-hex LAUNCH). 436 tests passing.
  - **Dead hand gets its own targeting, not `knownTargets`'s.** `adjudicate()` checks bunker outcomes before it ever reads a launcher count (`outcomes.ts`), so a launcher-contact kill during the final volley cannot change the verdict — only a hit on the enemy's real bunker can (Mutual Annihilation instead of losing outright). `deadHandOrders` targets known bunker/decoy sites only and blind-fires into the opponent's home zone otherwise; it never aims at a contact.
  - **A design bug caught before it shipped, not after:** the first draft ranked HARD's targets with a *stable* sort over an array where contacts were already pushed before static reveals, so the sort was a no-op and MEDIUM would have "preferred" contacts by sheer accident of insertion order — no actual behavioural difference between the two tiers. Fixed by making MEDIUM's selection distance-only (kind-blind) and HARD's priority-first, distance-tiebreak (`selectTarget`, both exported and unit-tested directly).
  - **Headless soak test, run and then deleted (not shipped):** 180 full CPU-vs-CPU matches through the real `resolve()` loop (20 seeds × all 9 difficulty pairings) — zero thrown errors, zero infinite loops. Found and worth flagging, not fixed this session: **zero DECAPITATION or MUTUAL_ANNIHILATION outcomes across all 180 matches** (128 Armistice, 44 Disarmament, 8 Mutual Disarmament). The drone heuristic (MEDIUM/HARD) always advances straight toward the front rather than sweeping side to side, so it essentially never explores enough of the enemy home zone's width to spot a bunker site — the CPU can play a whole match without ever attempting the bluff-test mechanic that is half the game. Not a bug (it does exactly what's documented); a tuning gap worth a follow-up session if CPU matches should exercise decapitation too. **→ Closed the same day — see the next entry. The soak was rebuilt as a committed tool rather than a throwaway, and the cause turned out to be two defects rather than the one guessed at here.**

- 2026-08-13: **balance harness + the CPU actually plays the game (`feature/cpu-bunker-hunt`).** Follow-up to the same day's CPU session, closing the decapitation gap it flagged. New `scripts/soak.ts` + `vitest.soak.config.ts` + `npm run soak`: plays whole CPU-vs-CPU matches through the real `resolve()` loop and reports outcomes, recon coverage, and offense — the measurement tool the last session wrote, used once, and deleted. It runs under Vitest because there is no TS script runner and adding one would be a new dependency; the separate config is what keeps `npm test` fast and the harness out of it. 448 tests passing (12 new); all four new invariants mutation-checked. No sim, spec or rules changes — this session is entirely `src/state/` and tooling.
  - **The gap was two defects, not one, and the harness named both.** (1) `advanceRow` was serving as the drone's goal as well as the launcher's, and it returns the *near* edge of the enemy home zone — so the drone flew to the boundary on round 2 and re-photographed one corridor for the rest of the match, covering **13%** of the zone it was supposed to be searching, and never flying deep enough to enter an interceptor bubble (0 drone deaths at medium/hard, so §11's "bases are inferred from `DRONE_DOWNED`" never fired either). (2) Even the 17/40 sides that *did* find a site never prosecuted it: launchers pushed at a generic row and HARD's targeting preferred a launcher contact over the site.
  - **Fixes.** `sweepLanes` + `nextSweepWaypoint`: a serpentine tour of the enemy home zone, derived from `RULES` rather than written down, that MEDIUM and HARD both fly — recon is not a difficulty setting, a drone that does not search is just broken. HARD's launchers now drive to within missile range of a known site (`AdvanceGoal`), and HARD's target ranking was **reversed** so a site outranks a contact.
  - **Result, measured over 100 matches per pairing.** Hard mirror: decapitations **0 → 45**, mean match length **22.1 → 15.3 rounds**, zone photographed **13% → 26%**, hits on the real bunker **0.10 → 0.45**. The game now reaches its own endgame instead of timing out.
  - **Head-to-head was added because the mirror numbers could not answer "is HARD actually harder?"** — and the first answer was no: hard vs medium came out **50–50**. That is what motivated the target-ranking flip (a HARD launcher that drove across the board to reach a site and then shot a passing contact had wasted the whole maneuver), which moved it to **58–49**. medium beats easy 133–0, hard beats easy 138–0.
  - **The harness holds an unfiltered `GameState` and that is not a licence** — see gotcha 39. The CPU players inside it are still handed `filterForPlayer` output and nothing else.

- 2026-08-13: **build-order step 10a — the order builder DONE (`feature/order-builder`).** The first of step 10's three sessions. **The game is now playable by a human**: you select one of your units, choose what it does, see the consequence drawn on the board, and the round resolves. Until now `resolveRound()` submitted `[]` for the human side every round, so the game was a spectator view of a CPU playing alone. New `src/state/belief.ts` and `src/state/orders.ts`, a new `orders` layer in `src/render/draw.ts`, and `src/ui/OrderPanel.tsx`. 496 tests passing (48 new); both required invariants mutation-checked. No sim, spec or rules changes — this session is entirely `src/state/`, `src/render/` and `src/ui/`.
  - **`belief.ts` is the lift, and it was proven inert before anything was built on it.** `believedState` and `knownEnemyHexes` moved out of `cpu.ts` verbatim; `cpu.test.ts` stayed green with **zero edits** and `SOAK_SEED=7 npm run soak` printed **byte-identical** numbers before and after. That pair is a stronger guarantee than any test written for the refactor, because it runs the real CPU through the real `resolve()` loop. The property it buys is worth naming: **the CPU and the human are now checked by the same import**, so "the CPU is bound by exactly the fog a human faces" is structural rather than a promise.
  - **The step-9 open question is answered.** "The validators take a `GameState` and the UI only has a `VisibleGameState`" is solved by belief, not by narrowing the sim — `moveTargets`/`launchTargets`/`flyTargets` each defer to the real `reachableHexes`/`validateMove`/`validateLaunch`/`validateFly`, and this session hand-rolled no legality anywhere. Narrowing the validators to `(units, map)` remains available as its own housekeeping session.
  - **Orders resolve themselves once every orderable unit is decided** — requested mid-session, and it forced a real design addition rather than a counter. Holding is a legal and often correct order (§3: a launcher with no order holds; §11: hovering is "a real choice, not a wasted round"), so "all orders submitted" could not mean "all four units have a MOVE/LAUNCH/FLY" — a player who deliberately held anything could never complete a draft. Hence `HoldEntry`: a decision that **submits nothing** and exists solely to make "I am finished with this unit" expressible. `draftOrders` strips it, so the engine never learns it exists. The manual **Resolve round** button stays, for going early with units left holding.
  - **`allDecided`'s `length > 0` guard is the one that would have bitten.** During the CPU's dead-hand round the human has zero orderable units, and "every one of zero units is decided" is vacuously true — without the guard the round would have resolved itself instantly and forever. The same guard covers a finished match.
  - **`resolveRound()` lost its `orders` parameter** and reads the draft instead; keeping both would have given the round two sources of truth. The three `match.test.ts` call sites migrated to `setOrder(order)` + `resolveRound()`, which is the better test — it exercises the path a click actually takes.
  - **Not built, deliberately:** setup placement (10b), hotseat handoff (10c), and animation from the event log. `sandbox.ts` still invents both setups.

### Next up: build-order step 10b — the setup placement UI (bunker → decoy → 2 interceptor bases per player, placed in secret, spec §12). Then 10c, the pass-the-screen hotseat handoff. **<- V1 done: playable and testable for fun.**

**Step 10 was split into three sessions: 10a order builder (DONE 2026-08-13) → 10b setup placement UI → 10c hotseat handoff.** `docs/next-session-10a.md` carried 10a's brief and was deleted on merge, as it instructed. Two follow-ups it flagged as out of scope are still open: **(1) `sandboxSetup` places by a fixed fraction of the legal-hex list** (bunker 0.5, decoy 0.2, bases 0.15/0.85), so the CPU's bunker lands near the middle of its home zone on every map — harmless for a first game, corrosive for repeat playtesting, because you learn where to point the drone and the bunker hunt stops being a hunt. The fix is small (derive the fractions from the match seed via `makeRng`) and belongs in its own change so the soak numbers move for one identifiable reason. **(2) Narrowing the sim validators** to `(units, map)` instead of a whole `GameState` — see the 10a entry.

**What step 9 left for step 10.** (1) ~~The validators take a `GameState` and the UI only has a `VisibleGameState`~~ — **CLOSED by 10a.** `src/state/belief.ts` is the "client-side board as I believe it to be" shape the note called for, and it is shared by the CPU and the human rather than duplicated. The move highlight is a prediction, and `OrderPanel` says so in words next to it. (2) The viewer switch in the HUD is a sandbox control, not the handoff; **10c** replaces it with a real pass-the-screen sequence. (3) `sandbox.ts` is a fixture that invents both setups — **10b**'s placement UI replaces the human side and it stays only as a solo mode. (4) Nothing animates yet: the client re-renders the final state and appends the log, where §6's whole point is that clients animate *from* the event list. That is a step-10-or-later polish item, and the log is already in the right shape for it.

**Gotchas for future sessions — each of these is load-bearing, don't "simplify" any of them:**
1. The decoy is immobile and **blocks hexes** exactly like any ground unit. If it were passable, walking a launcher through a site would identify the fake for free.
2. The decoy never emits `BUNKER_HIT` — it dies to the hit. That silence-vs-destruction difference *is* the tell that identifies the real bunker. (The code's branch deliberately names bunker **and** decoy, so §12's "raise the decoy to 2 HP" lever stays symmetric by construction; at 1 HP the decoy can never reach it.)
3. Destroying a decoy triggers no dead hand and satisfies no win condition.
4. **Hits stack within a round** (spec §3): two missiles on a full-health real bunker kill it outright, so the impact phase must total damage per hex, not apply one hit per hex.
5. **`IMPACT` fires even when the target hex is empty** (spec §6). If it only fired on hitting something, its presence would leak occupancy and blind-fire probing would locate bunkers and bases for free.
6. **The dead-hand round is launches only** (spec §3): phases 2→3 only, no recon phase, no movement phase, no orders from the opponent.
7. **Terrain is public** (spec §11). Hidden information covers assets, never tiles — the visibility filter must never strip or mask `MapData`.
7b. **`groundPassable` is not "can this be built here"** (spec §2, §12). V1 has two terrains, and mountains block *movement only*: bunker, decoy and interceptor base may all be built on them. Placement validation must read `RULES.placementTerrain`, never `TerrainDef.groundPassable` — the field was renamed from `passable` precisely so that mistake looks wrong. The corollary, now live in `missiles.ts`: **missile targeting and flight never filter by terrain**, or a mountain bunker becomes invulnerable (spec §10).
7c. **`generateMap` validates and re-rolls; never patch a bad map.** Mountains are grown as ranges, and unlike scattered noise a ridge can wall the board off, so every generated map is checked for coverage, spawn connectivity and approach cost (spec §7). Carving a pass through an offending ridge is the tempting fix and it is a trap — every carve must be applied to a hex *and its half-turn twin* or the two players silently get different maps.
8. **Launcher sightings expire after one order phase; static sightings never do** (spec §11). The intel state therefore has two parts with different lifetimes: a permanent set of revealed static assets, and a this-round-only set of launcher contacts that is rebuilt from scratch every resolution. Don't "improve" this by keeping stale launcher markers with a round number — persistent ghosts were deliberately cut; the event log is the history.
9. **The event log is append-only and permanent** (spec §6). Every launch a player detected stays readable for the whole match. Expiring a map marker must never delete a log entry.
10. **`UNIT_DEFS.drone.movement` is a straight-line flight range, not a ground budget** (spec §11). Never feed the drone to the ground-movement flood fill — it ignores terrain and units entirely. `reachableHexes` returns only its own hex for a drone and `validateMove` rejects drone MOVE orders with `AIR_UNIT`; the drone moves by FLY, resolved with `hexLine`.
11. **The board is fought north/south and its width MUST stay even** (spec §7). Territory splits by *row*: P1 south (rows 13–18, high numbers, advancing north), P2 north (rows 0–5). Hexes are flat-top precisely so the advance axis has true N/S neighbours. Map symmetry is a **180° rotation, not a mirror** — on flat-top odd-q hexes a top/bottom mirror shears the grid and hands the two players subtly different distances, and the half-turn is only an isometry while the width is even (hence 16, not 15). `generateMap` throws on an odd width and `map.test.ts` checks distance preservation over every hex pair. Consequence to expect, not "fix": the two sides' launcher *columns* differ (P1 col 2 answers P2 col 13).
12. **`hexLine` includes both endpoints, and the two callers slice it differently** (spec §10, §11). A missile checks for interception on every hex *after* the origin — `slice(1)`, keeping the target hex — while the drone's reveal swath covers the **whole** array including its start hex. Getting this backwards makes launchers shoot themselves down or blinds the drone to where it took off from. Never re-derive a line anywhere else (UI preview included) — call `hexLine`, or the pinned epsilon stops guaranteeing anything.

13. **`MOVE_FAILED` fires for exactly two rejection reasons — `TILE_OCCUPIED` and `OUT_OF_RANGE`** (spec §9). Every other rejection is dropped silently. This looks like an inconsistency worth "tidying up" and is not: `MOVE_FAILED` names a unit id, so answering an order that named an *enemy* unit would put a real enemy id into the sender's log — the trackable identity §11 exists to withhold. Once that case must be silent, reporting the other impossible-for-an-honest-client cases only makes "your advance met resistance" ambiguous between real contact and a UI bug. The two reported reasons are exactly the ones hidden information can cause.
14. **Movement events are emitted in `GameState.units` order, never order-array order** (spec §9). Both are deterministic, but only the canonical one makes the same physical outcome produce the same log no matter how a client sorted its submission — which is what the §6 determinism test is actually asserting. `resolve.test.ts` submits orders b-then-a and expects the log a-then-b; that test is the guard.
15. **`ordersPerUnit` is counted across ALL order kinds, not just MOVEs** (spec §3, §9). That is what makes move-XOR-launch structural — a launcher handed a MOVE *and* a LAUNCH is over budget and does nothing. Counting only MOVEs would look correct today and silently let that pair through the moment step 6 implements launches. An over-budget unit holds and emits nothing; the engine never guesses which order was meant. The budget is now applied **once** at the top of `resolve()` and the resulting `OrderBook` is shared by every phase — never re-derive it per phase, or a unit could be over budget in one phase and not another.

16. **`DroneFlight.path` is what the drone TRANSMITTED, not where it flew** (spec §11). On a downed flight it stops one hex short of the kill, and the death hex arrives separately in `DRONE_DOWNED`. This is why the reveal swath can be built from `path` with no special case — the "a shot-down drone reveals nothing from its death hex" rule is baked into the data rather than left to each caller. Note what it does *not* mean: the death hex itself is still spotted, from the corridor of the last hex the drone safely reached. What is lost is everything beyond it.

17. **A hovering drone still watches, and a rejected FLY is silent** (spec §11). No order, an invalid order and an over-budget order all resolve identically — a zero-length flight that transmits its own hex's corridor. Do not "tidy" this into an early `continue` that skips the swath: it would make hovering strictly worthless and quietly delete the reveal a player gets simply by having a drone alive. There is deliberately no air-layer `MOVE_FAILED`, because every FLY rejection is derivable from public or own-side information.

18. **The drone never blocks ground movement** (spec §2, §9). `occupiedHexes` skips `kind === 'drone'`, and that line is load-bearing: a drone that blocked would be a third detector by the back door — park it on a hex, watch an enemy launcher's advance fail, and you have located a unit no rule permits you to see. This was an actual bug fixed in step 5; it looked harmless for two build steps because drones never moved.

19. **The respawn tick is NOT one of §3's five phases** (spec §11). It runs as the resolution hands over to the next order phase, which is exactly what makes "one full blind round" come out right *and* what puts the replacement drone on the board in time for the player to give it an order. Moving it to the top of `resolve()` would look equivalent and would silently cost the player a round: the drone would exist during resolution but never during an order phase.

20. **Recon can never photograph an interceptor base at the shipped radii, and that is the design** (spec §11, decided 2026-08-11). `reconSwathRadius` (1) ≤ `interceptorCoverageRadius` (1) means any base near enough to enter the swath is covering a hex the drone must enter to see it, so the drone always dies one step early — brute-force verified, not a hunch. Bases are found by inference from `DRONE_DOWNED` and `MISSILE_INTERCEPTED` instead. Changing either radius is a board-wide balance change for playtest, not a fix; read the notes on both constants in `defs.ts` first.

21. **Owner-only events carry `owner`, and `ASSET_SPOTTED` is the trap** (spec §6). The visibility filter is handed the log and a player id and nothing else, so it cannot look a `unitId` up in a state to learn whose event it is — the audience must be on the event. Any new owner-only event needs an `owner` field or step 8 cannot route it. **But `ASSET_SPOTTED.owner` is the owner of the asset that was *seen*, so its audience is that player's opponent.** It is the single event where reading `owner` as "who may see this" is exactly backwards, and it is the one place a plausible-looking filter written from the pattern will silently show each player their own assets and hide the enemy's.

21b. **`LAUNCH_DETECTED` marks the DEFENDER's map — `intel[opponentOf(missile.owner)]`** (spec §11). Same trap family as 21: intel is keyed by the *viewer*, so `intel.p1` is what p1 knows about p2. Filing a launch contact against the firer would give each player a map of their own launches and hide the enemy's — and it would look perfectly reasonable in the diff, because `missile.owner` is right there. The rule to hold onto: nothing a player does ever writes to their own intel.

22. **The simultaneous-missile tiebreak is the ORIGIN HEX, never the launcher's unit id** (spec §10, changed in step 6). Ordering by id is just as deterministic and was the original wording — but the order is publicly observable, so it would leak an ordering of enemy launcher ids and, over a few rounds, let the defender track a specific launcher across the match. That is the trackable identity §11 keys every pile of intel by hex to withhold, and §6 already bans it for missile ids. `compareHex` exists for exactly this. **Bases keep the id tiebreak** (`basesCovering`), because the defender owns them, no event names one, and a base cannot move — so there is no identity to track.

23. **A base is self-protecting: one missile aimed at a base is always intercepted** (spec §10). A missile must cross a base's own bubble to reach it, so killing a base takes a **saturating volley of 2+ in one round**. This is the cap's whole purpose, not a bug — if a test or a UI hint assumes one missile kills a base, the test is wrong. (This bit during step 6: a test fixture had a lone missile "destroy" a base and the engine correctly ate it.)

24. **Interception advances every missile one step at a time, together** (spec §10). A base spends its single intercept on whichever missile *enters* its coverage first, so a missile two hexes out cannot be saved by one that would only have arrived later. Resolving one whole flight at a time looks equivalent and silently awards the intercept to whichever missile the array happened to list first — nondeterminism by array order, which is the exact class of bug §6 is guarding against.

25. **Phase 3 skips drones and corpses** (spec §2, §3). Missiles cannot touch a drone even when it is hovering directly over the target hex — the air layer is absolute — and an already-destroyed unit takes no second hit. Damage is totalled per *hex* before anything is applied, because the missile is aimed at ground, not at a target: whatever is standing there takes it, friendly or enemy.

26. **A round that ends at phase 4 stops there — no movement, no respawn tick** (spec §3, amended in step 7). §3 said this of dead hand only; it now applies to any verdict. It changes no outcome (nothing in movement can affect a §4 condition) and it is not tidiness: the log is what clients animate, so units moving after `GAME_OVER` would be a match that visibly carries on past its own ending. The rule players feel is that **the round which decapitates someone is a round in which nobody moves** — you cannot drive out of the dead-hand volley you just provoked.

27. **`adjudicate` reads `state.phase`, and that is the whole dead-hand distinction** (spec §4). One destroyed bunker means DEAD_HAND in a normal round and DECAPITATION in the final one — the *same board*. Do not "simplify" this into a boolean parameter threaded from resolve(): the state machine's current node is the fact, and a flag is a fact that can be passed wrongly.

28. **Absence is not destruction** (spec §4, ruled in step 7). A side with no bunker unit is not decapitated; a side with no launcher units is not disarmed. `startMatch` guarantees a real match never hits either case, so this exists to keep the engine inert on partial test boards — which is load-bearing in practice: without it, most of the 335 tests would end their match at phase 4 before reaching what they were testing. If you ever build a fixture where one side's only launcher is a corpse, that board is a disarmament and the round will stop; `resolve.test.ts`'s `spare()` helper is the fix.

29. **The dead-hand round gets its own round number; a finished match does not** (spec §3, §6). Dead hand increments because missile ids are `r{round}@{origin}` and the log keeps both rounds forever — reuse would collide. Game over freezes the counter, because `round` means "the round being played" and none is. Both are deliberate, and they are different on purpose.

30. **Placement validation must never look at the opponent's setup** (spec §12). It has no parameter for it, and that is the design: the home zones are disjoint (§7) so there is nothing to collide with, and a validator that consulted enemy placements would turn `legalPlacementHexes` — the setup UI's highlight overlay — into a third detector, showing the enemy's secret sites as holes in your own legal ground.

31. **The filtered state drops enemy units; it does not mask them** (spec §6, ruled in step 8). `VisibleGameState.units` holds the viewer's own units only, and the entire enemy picture is `intel`, keyed by hex. The tempting alternative — keep enemy units in the array with `kind: 'decoy'` rewritten to `'bunker'` — is a leak wearing a mask: it puts real enemy `UnitId`s into the client, which is the cross-round trackable identity §11 keys every pile of intel by hex to withhold, and it makes `hp` and `kind` into fields the engine has to lie in. As written, `VisibleGameState` has no field an enemy id *could* live in, so the guarantee survives edits by people who have not read this line.

32. **`visibility.ts` decides nothing — if you are computing visibility there, the rule is in the wrong file** (spec §6, §11). `resolve()` owns all the bookkeeping: it files contacts, rebuilds the one-round pile from scratch every resolution, and calls `forgetDestroyed` when a unit dies publicly. `state.intel[playerId]` is therefore *already* exactly what that player knows, and the filter is a projection of it. This is also why the filter is short: it is the boundary, not the brain.

33. **The filter is a projection, not a deep clone, and that is safe only while the render layer keeps its side of the bargain** (CLAUDE.md architecture rule). Arrays are rebuilt so nobody can splice into the real state, but the `Unit` objects inside are shared by reference, because copying every unit on every frame is waste. `src/render/` and `src/ui/` read state and never mutate it — if that rule is ever bent, this is one of the places it goes wrong quietly.

34. **The one visibility guarantee no sim test can protect is that the renderer never sees a raw `GameState`** (closed in step 9). Every leak `visibility.ts` prevents is prevented only for callers that actually call it; handing `GameCanvas` the unfiltered state bypasses the whole layer without failing anything in `src/sim/`. It is now structural: see 35.

35. **`truth` in `src/state/match.ts` is a module-private `let`, and it must stay one** (spec §6). It is deliberately NOT in the Zustand state and has no accessor, so there is no hook, selector, component or test that *can* reach an unfiltered `GameState` — the only way to leak one is to edit that file on purpose. Putting it in the store "for devtools" or exporting a `getTruth()` "just for a test" re-opens the exact hole step 9 was built to close, and nothing would fail when it did. Tests assert through `viewFor`/`logFor`, which is also how the UI reads.

36. **In hotseat one machine holds both players' views and both logs — that is what a pass-the-screen handoff is.** The redaction is `views[viewer]`, so any component reading `views` directly, or a log by a hardcoded player id, silently shows the wrong player their opponent's picture. `useView`/`useLog` key off `viewer` for that reason; add no hook that takes a `PlayerId` from a caller. In V1.5 the same split becomes physical (server keeps `truth`, each client gets its own view) and this class of bug stops being possible.

37. **`hexesInRange` is pure geometry and returns hexes off the edge of the board.** The sim leaves them in on purpose — they can never match a unit, so filtering would cost a map lookup per hex to change nothing (`reconSwath`) — but anything that *draws* from it must filter with `tileAt` or it paints ground that does not exist. This was a live bug in step 9's coverage overlay, found by looking at a screenshot; no test would have caught it. `tileAt` is the only correct on-map test, because the map is a rectangle in col/row and a slanted parallelogram in axial.

38. **Every event the client sees goes through `filterEventsForPlayer`, including the one the store invents.** A resignation is not a board fact, so `resign()` writes `truth` and synthesises the `GAME_OVER` event itself (spec §4) — and then publishes it through the filter like any other, rather than pushing it straight into both logs because it is "obviously public". Who may see an event is §6's judgement and is already written down in one place; a second place that decides it is a second place to get it wrong.

39. **`scripts/soak.ts` holds an unfiltered `GameState`, and that is not a precedent for anything under `src/`** (CLAUDE.md gotchas 34/35). Measuring the game requires ground truth — "did anyone ever find the *real* bunker" is a question no redacted view can answer — so the harness is deliberately outside `src/`, renders nothing, and returns nothing to the client. The part that keeps it honest: **the CPU players inside it are still handed `filterForPlayer` output and nothing else**, exactly as the store hands it to them. The instrument sees through the fog; the players never do. Anything under `src/` reaching for truth is still the hole step 9 closed.

40. **`sweepLanes` runs its lanes edge to edge, NOT inset by the swath radius** (spec §11). Insetting by `reconSwathRadius` is the obvious move and it silently loses the board's outermost column: the swath spreads `radius` columns sideways, but on staggered odd-q columns that spread is a *diagonal*, so a lane at column 1 photographs (0, row) and (0, row+1) while leaving (0, row-1) unseen — and a bunker can legally sit there. Flying the edge itself costs nothing. The test that catches this asserts the whole zone is covered rather than checking lane positions, so a future retune of the radius, the drone's range or the home zone either still covers the zone or fails loudly.

41b. **HOLD is a draft entry that submits nothing, and deleting it breaks the auto-resolve rule, not just a button** (spec §3, §11; added in step 10a). The round resolves the moment every orderable unit is decided — and holding is a legal, often correct order, so without an explicit way to *say* "this launcher stays put" a player who wanted to hold anything could never complete a draft. `draftOrders` strips HOLD before submission, so the engine never learns it exists: a unit it receives no order for holds anyway. The tempting simplification is "just count units with real orders", and it silently deletes holding as a strategy.

41c. **`allDecided` returns false on an EMPTY orderable set, and that guard is why the game does not lock up** (spec §3). During the opponent's dead-hand round you have no orderable units at all, and "every one of zero units is decided" is vacuously true — auto-resolve would fire on an empty draft, resolve, find the same condition, and never stop. A finished match is the same shape. `orderable.length > 0` reads like a defensive nicety and is the opposite.

41d. **Order legality is checked against `SANDBOX_PLAYER`'s view, never the current `viewer`'s** (spec §6; `orderingView` in `match.ts`). The viewer switch is a debug control, so a player glancing at the CPU's board must not thereby be drafting orders for the CPU. It needs no ownership check to enforce that: a `VisibleGameState` holds only its owner's units, so an enemy unit id simply is not found. The UI *additionally* disables entry while spectating — two independent guards, one stopping a wrong order being stored and one stopping it being offered, and neither is redundant.

41e. **`modesFor` carries the phase rules that the sim's validators do not know** (spec §3). `validateMove` will happily accept a launcher MOVE during a dead-hand round, because the sim's order validators are phase-blind — §3's "the dead-hand round is launches only, and the opponent issues no orders at all" lives in `modesFor`, and `isLegalOrder` checks it *before* calling the validator. Dropping that guard looks like removing a redundant check and lets a MOVE into a round that has no movement phase.

41. **The soak harness gives the two CPU sides DIFFERENT rng streams.** Seeding both from `seed + round` (which is what `match.ts` does for its single CPU, correctly) hands them identical random sequences — and for EASY, the one tier that actually rolls dice, that makes the two players mirror each other's coin flips and quietly halves the sample. Cross-tier runs play every pairing in **both seats** for the same reason: a seat advantage would otherwise be reported as skill.

### Open questions
- **SETTLED 2026-08-12: no partial advance. §9 stands exactly as written — a blocked move fails entirely and the launcher holds.** Raised as "stopping at the last open hex so the round isn't wasted", explored, and withdrawn on the reasoning §9 already gives: **advancing into unscouted ground has to risk something, and that risk is what makes the drone worth flying.** Three things a future session should know before re-opening it. (1) **There is no path in this engine.** `reachableHexes` is a flood fill that treats occupied hexes as walls, so an enemy *between* you and your destination is routed around invisibly and costs nothing. A move is wasted in only two cases: you ordered the launcher **onto** a hidden unit's hex, or (rarely, in a chokepoint) an unseen blocker cut every route within budget. It is not "I bumped into something on the way" — that cannot happen. (2) **Walk-and-stop would need a canonical path** plus an equal-cost-route tiebreak, a UI path preview, and both reproduced identically on the V1.5 server — and it would let one order probe a whole lane and name the exact hex that stopped it, a bunker-finding detector competing directly with the drone (§11, §12). (3) A pathless variant (land on the reachable hex nearest the destination) leaks nothing new and was the option on the table, but it makes probing **cheap**, which is the same objection in weaker form: today driving at a suspected hex costs a full round when you are right. If playtest shows the wasted round is merely annoying rather than tense, that variant is the first thing to try — and standoffs would have to fall back identically, or a partial advance becomes a free tell that an enemy moved at you (gotcha 13).
- **SETTLED 2026-08-11 (start of step 6): recon can never photograph an interceptor base, and that is now the design.** The radii stay at 1. Bases are found by *inference* from the two public 7-candidate clues (`DRONE_DOWNED`, `MISSILE_INTERCEPTED`), so a drone lost over enemy ground is a bearing on a base — and, via §12's ≥3 exclusion, a shadow on the bunker. Spec §11's OPEN QUESTION section is now "Bases are inferred, never photographed"; §12's design note and the `defs.ts` notes on both radii were reworded to match. Revisit in playtest (step 10), never as a drive-by fix: raising `reconSwathRadius` to 2 widens *every* reveal from a 3-wide corridor to a 5-wide one and shortens the bunker hunt, which is the game's clock.
- **NEW 2026-08-13: ~46% of CPU matches still end in Armistice, and only half of all sides ever find a site.** With the sweep working, the limit is now the drone's survival rather than its search pattern: it dies 2.8 times a match (2 blind rounds each), so it photographs ~26% of the enemy home zone before the clock runs out. That is arguably §11 working exactly as designed — recon is *supposed* to be taxable — but it is the first thing the harness should be pointed at when tuning begins. The levers, in the order worth trying: `roundCap` (25), `droneRespawnDelay` (2), `reconSwathRadius` (1 — but read gotcha 20 and the `defs.ts` note first; it is a board-wide change, not a fix). Do not tune any of them without a before/after `npm run soak`.
- **NEW 2026-08-13: HARD beats MEDIUM only 58–49 of 200 (93 draws).** Better than the 50–50 it started at, but the easy→medium gap (133–0) is enormous and the medium→hard gap is slim. If a genuinely distinct third tier matters, the untried ideas are the deliberate 2-missile alpha strike on a site (spec §7/§12 — two hits in one round kill a full-health bunker outright and skip the decoy test) and firing from inside one's own interceptor coverage. Both are real tactics the spec names and no tier currently plays.
- All §7 numbers remain untested first drafts (validate by playtest after step 10) — but `npm run soak` now measures the consequences of changing one. The §10 intercept tiebreak is accepted-arbitrary by design — but it is now **origin hex, not unit id** (changed in step 6, see above): arbitrary is fine, publicly-observable-and-identity-bearing is not.

### Known issues
- None. The code/spec mismatch previously listed here was closed when build-order step 2 (pivot migration) landed on 2026-08-11 — `main` now carries post-pivot design *and* post-pivot code, as the status header above states.
